/**
 * Return an iterable that runs given amount of times.
 * Use for:
 * ```
 * for (const x of times(5)) {
 *   // Do some code here
 * }
 * ```
 *
 * @param {number} n
 * @returns
 */
export declare function times(n: number): Iterable<number>;
//# sourceMappingURL=various-utils.d.ts.map